[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "voxcpmane2"
version = "0.1.3b1"
description = "VoxCPM2 TTS runtime + HTTP server for Apple Neural Engine"
requires-python = ">=3.10,<3.13"
license = { text = "MIT" }
readme = "README.md"


dependencies = [
    "numpy>=2",
    "ml-dtypes>=0.5.0",
    "soundfile",
    "soxr>=1.0.0",
    "tokenizers",
    "fastapi",
    "uvicorn",
    "aiofiles",
    "huggingface_hub",
    "sounddevice",
    "tqdm",
    "ftfy>=6.3.1",
    "inflect",
    "wetext",
    "regex",
]

[project.optional-dependencies]
asr = [
    "faster-whisper>=1.0",
    "ctranslate2>=4.0",
]
development = [
    "safetensors>=0.4.0",
]

[project.urls]
Homepage = "https://github.com/0seba/VoxCPMANE"

[project.scripts]
voxcpmane2-server = "voxcpmane.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/voxcpmane"]
